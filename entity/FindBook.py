class FindBook:
    def __init__(self, id=0,name="", auhtor="", sourceUrl="", wid=0, sid=0, isFinish=0, sourceWeb=0, targetUrl="",targetWeb=""):
        self.id = id  # 如果需要设置默认值的属性，可以在这里初始化
        self.name = name
        self.auhtor = auhtor
        self.sourceUrl = sourceUrl
        self.wid = wid
        self.sid = sid
        self.isFinish = isFinish
        self.sourceWeb = sourceWeb
        self.targetUrl = targetUrl
        self.targetWeb = targetWeb